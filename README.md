# Dataset-Curation

Make sure to first download the full sha1 corpus from MalwareBazzar.
Sha1 was used as opposed to sha256 because said corpus was faulty.

```bash
# Install dependencies
pip install -r requirements.txt

python main.py
```

